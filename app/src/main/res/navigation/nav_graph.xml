<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ilksayfam">

    <fragment
        android:id="@+id/ilksayfam"
        android:name="com.isaacer.cicekbahcem.ilksayfam"
        android:label="fragment_ilksayfam"
        tools:layout="@layout/fragment_ilksayfam" >
        <action
            android:id="@+id/action_ilksayfam_to_secondFragment"
            app:destination="@id/secondFragment" />
    </fragment>
    <fragment
        android:id="@+id/secondFragment"
        android:name="com.isaacer.cicekbahcem.secondFragment"
        android:label="fragment_second"
        tools:layout="@layout/fragment_second" >
        <argument
            android:name="isim"
            app:argType="string" />
        <argument
            android:name="aciklama"
            app:argType="string" />
        <argument
            android:name="image"
            app:argType="integer" />
    </fragment>
</navigation>